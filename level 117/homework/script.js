class MathHelper {
    static Pi = Math.PI;  
    
    static power(base, exponent) {
        return Math.pow(base, exponent);
    }

    static sqrt(number) {
        return Math.sqrt(number);
    }
}
